import 'package:flutter/material.dart';

class ComponentRadius {
  final BorderRadius radius100 = BorderRadius.circular(100);

  final BorderRadius radius52 = BorderRadius.circular(52);

  final BorderRadius radius39 = BorderRadius.circular(39);

  final BorderRadius radius24 = BorderRadius.circular(24);

  final BorderRadius radius12 = BorderRadius.circular(12);

  final BorderRadius radius8 = BorderRadius.circular(8);

  BorderRadius custom(double radius) => BorderRadius.circular(radius);
}
